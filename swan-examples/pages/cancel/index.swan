<view class="container">
  <view class="page__hd">
    <view class="page__title">axios.cancel</view>
  </view>
  <view s-if="{{begin}}">request pending</view>
  <view s-if="{{cancel}}">request canceled</view>
</view>
